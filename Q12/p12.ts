/*
12. Greetings: Start with the array you used in Exercise 11, but instead of just printing each person’s
name, print a message to them. The text of each message
should be the same, but each message should be personalized with the person’s name. 
*/
let friend_names = ['Ali', 'nasir', 'hamza', 'umer','waseem','asfar','ahmed' ];
console.log(`Hello ${friend_names[0]} how are you? how is the analysis going? `);
console.log(`Hello ${friend_names[1]} how are you? how is the analysis going? `);
console.log(`Hello ${friend_names[2]} how are you? how is the analysis going? `);
console.log(`Hello ${friend_names[3]} how are you? how is the analysis going? `);
console.log(`Hello ${friend_names[4]} how are you? how is the analysis going? `);
console.log(`Hello ${friend_names[5]} how are you? how is the analysis going? `);
console.log(`Hello ${friend_names[6]} how are you? how is the analysis going? `);